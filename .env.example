# =============================================================================
# Slimming Gym Fitness - Variables de Docker Compose
# =============================================================================
# Copia este archivo como .env en la raiz del proyecto y completa los valores.
#
#   cp .env.example .env
#
# El .env NUNCA se versiona (esta en .gitignore): contiene credenciales.
# Este archivo es distinto de backend-gimnasio/.env, que se usa cuando se
# ejecuta la API directamente con `node server.js` sin contenedores.
# =============================================================================

# --- Base de datos ---
# Contrasena del usuario 'sa' del contenedor de SQL Server. SQL Server exige al
# menos 8 caracteres con mayusculas, minusculas y numeros o simbolos.
MSSQL_SA_PASSWORD=

# Credenciales con las que la API se conecta. En produccion deberia ser un
# usuario de aplicacion con permisos minimos (db_datareader + db_datawriter +
# EXECUTE), nunca 'sa'. Ver database/README_INSTALACION_VM.md
DB_NAME=GymDatabase
DB_USER=slimming_app
DB_PASSWORD=

# --- Seguridad (OBLIGATORIAS) ---
# La API no arranca si JWT_SECRET falta o mide menos de 32 caracteres.
# Genera cada secreto con:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
# Secreto compartido que valida el webhook de la pasarela de pagos
# (cabecera x-webhook-secret). Sin el, el webhook rechaza toda peticion.
WEBHOOK_SECRET=

# --- Dominio ---
# Con Nginx sirviendo el frontend y la API bajo el mismo dominio, el navegador
# no realiza peticiones de otro origen, asi que CORS practicamente no interviene.
# Se mantiene la lista blanca para clientes externos.
FRONTEND_URL=https://tu-dominio.com
ALLOWED_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com

# --- Correo electronico (Nodemailer / SMTP) ---
# Para Gmail se requiere una "App Password", no la contrasena de la cuenta.
SMTP_EMAIL=
SMTP_PASSWORD=

# --- Zona horaria ---
# Los contenedores corren en UTC por defecto y NO heredan la zona del host.
# Sin esto, el control de ingreso marca como vencidas membresias vigentes a
# partir de las 19:00 hora de Ecuador y la bitacora del dia sale vacia.
TZ=America/Guayaquil

# --- Limite de memoria de SQL Server (MB) ---
# Sin techo, reserva toda la RAM que ve y deja sin margen a la API y a la
# compilacion del frontend. En una VM de 8 GB, 3072 es un valor razonable.
MSSQL_MEMORY_LIMIT_MB=3072

# --- PayPal (pago en linea) ---
# 'sandbox' para pruebas con dinero ficticio, 'live' para cobros reales.
PAYPAL_MODE=sandbox
# Client ID: es publico. El navegador lo obtiene por GET /api/payments/paypal/config
PAYPAL_CLIENT_ID=AdQ4MxUpGgvGzP4Km5Bc3NYa8_ah574VsGMeqwWZ8wQjrNDKhnS7yJ4X3LoRcdQ5mlFOFsbEmCQy63EW
# Secret: NUNCA debe salir del servidor. Permite crear y cobrar ordenes.
PAYPAL_CLIENT_SECRET=EHKLGJ97tZdlIl6_ikJ4BVLpVkRDd4r_dZfea2ppospXTAld6e7A3ZSL3p4XbL1sYtp7hluaUMUhvIdw
# Identificador del webhook, necesario para verificar la firma de los eventos.
# Se obtiene al crear el webhook en el panel de desarrollador de PayPal.
PAYPAL_WEBHOOK_ID=
# Ecuador usa dolar, asi que no hay conversion de moneda.
PAYPAL_CURRENCY=USD

# NOTA: el usuario y contrasena de las cuentas de prueba de Sandbox NO van aqui.
# Sirven para iniciar sesion en sandbox.paypal.com y simular al comprador.
