steps:
  # 1. Instalar dependencias y compilar Vite
  - name: 'node:20'
    entrypoint: 'sh'
    args:
      - '-c'
      - |
        cd frontend-gimnasio
        npm ci
        npm run build
        npx firebase-tools deploy --only hosting --project eastern-automata-txsl0
